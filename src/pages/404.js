import React from 'react';
import Layout from '../components/layout';

const NotFoundPage = () => (
  <Layout title="404">
    <h2>You confused my site!</h2>
    <p>What the heck. Just try to find your way via the menu above.</p>
  </Layout>
);

export default NotFoundPage;
